# Node Version Manager 
[[ -s /Users/hlawson/.nvm/nvm.sh ]] && . /Users/hlawson/.nvm/nvm.sh
 
# Ruby Version Manager
[[ -s "/Users/hlawson/.rvm/scripts/rvm" ]] && source "/Users/hlawson/.rvm/scripts/rvm"
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm" 

# Bash
alias br="source ~/.bashrc"
alias ba="vim ~/.bash/aliases && br"
alias bc="vim ~/.bash/config && br"
alias be="vim ~/.bash/env && br"
alias c=clear

# List files
alias l="ls -lGh"
alias la="l -a"

# History
alias th=bash_top_history

# Postgress
alias postgres_start="pg_ctl -D /usr/local/var/postgres -l /usr/local/var/log/postgres/postgres.log start"
alias postgres_stop="pg_ctl -D /usr/local/var/postgres stop -s -m fast"

# Open
alias o="/usr/bin/open"

# Vim
alias v="vim"

# Utility
alias t="take"
function take() { mkdir -p "$@" && cd "$@"; }
alias lol='echo "hahahaha"'

# Notes
function n() {
	local NOTES_DIR=/Users/hlawson/ThoughtWorks/LearningProjects/notes;
	local GIT_ARGS="--git-dir=$NOTES_DIR/.git --work-tree=$NOTES_DIR";
	local NOTES_FILE=notes
	vim $NOTES_DIR/$NOTES_FILE && git $GIT_ARGS add $NOTES_DIR/$NOTES_FILE && git $GIT_ARGS commit -am "Auto commit";
}
